# Frontend Environment Variables
# Copy to .env.local and fill in values

# Backend API base URL
# Leave empty to use the built-in Next.js proxy (recommended for local development).
# Set to the full backend URL (e.g. http://localhost:8000) when running the
# frontend and backend on different origins (the proxy is still active as a
# fallback regardless).
NEXT_PUBLIC_API_BASE=
